export type Colors = {
  mainHeader: string;
  mainHeaderShadow: string;
  menuButtons: string;
  menuButtonsBorder: string;
  questionCorrectFrom: string;
  questionInorrectFrom: string;
  questionDefaultFrom: string;
  questionCorrectTo: string;
  questionInorrectTo: string;
  questionDefaultTo: string;
};
export const colors: Colors = {
  mainHeader: "hsl(325, 100%, 50%)",
  mainHeaderShadow: "hsl(191, 100%, 32%)",
  menuButtons: "hsl(62, 100%, 78%)",
  menuButtonsBorder: "hsl(52, 100%, 59%)",
  questionCorrectFrom: "#56FFA4",
  questionInorrectFrom: "#FF5656",
  questionDefaultFrom: "#56ccff",
  questionCorrectTo: "#59BC86",
  questionInorrectTo: "#C16868",
  questionDefaultTo: "#6eafb4",
};
export const shadow = {
  boxShadow: "0px 5px 10px rgba(0, 0, 0, 0.25)",
};
